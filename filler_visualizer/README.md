Set env variable for the sdl library
```bash
export LIBRARY_PATH="/opt/homebrew/lib"
export CFLAGS="-I/opt/homebrew/include"
```

command to run the program
```bash
cargo run ../logs/game_log.txt
```
